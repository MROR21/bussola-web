# Copie pra .env.local (git-ignorado) e preencha com o Client ID do app registrado no Azure
# (Entra ID > Registros de aplicativo > Bússola > Application (client) ID).
# Sem essa variável, o botão "Entrar com Microsoft" fica escondido — o resto do app funciona normal.
VITE_MSAL_CLIENT_ID=
